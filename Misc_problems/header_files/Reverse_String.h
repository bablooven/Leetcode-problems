#ifndef REVERSESTRING_H    // To make sure you don't declare the function more than once by including the header multiple times. //if not defined
#define REVERSESTRING_H

#include<string>
using namespace std;

string reverseString(string str);

#endif